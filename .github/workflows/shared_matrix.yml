name: test shared matrix
on:
  push:
    branches:
      - main

jobs:
  #builder:
  #  name: set matrix
  #  runs-on: ubuntu-latest
  #  strategy:
  #    matrix:
  #      cfg:
  #        - data:
  #          - { builder-image: paketobuildpacks/builder-jammy-tiny:0.0.175 }
  #          - { builder-image: paketobuildpacks/builder-jammy-tiny:0.0.173 }
  #  outputs:
  #    matrix: ${{ steps.set-matrix.outputs.matrix }}
  #  steps:
  #    - id: set-matrix
  #      #env:
  #      #  CFG: ${{ toJSON(matrix.cfg) }}
  #      run: |
  #        # jq -r -n  'env.CFG | fromjson | @json "result=\(.data)"'
  #        #echo "name=[{"builder-image":"paketobuildpacks/builder-jammy-tiny:0.0.175"},{"builder-image":"paketobuildpacks/builder-jammy-tiny:0.0.173"}]" >> $GITHUB_OUTPUT
  #        echo "Do nothing"
  job1:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - id: set-matrix
        run: |
          echo "matrix={\"include\":[{\"project\":\"foo\",\"config\":\"Debug\"},{\"project\":\"bar\",\"config\":\"Release\"}]}" >> $GITHUB_OUTPUT
          echo "matrix={\"include\":[{\"builder-image\":\"paketobuildpacks/builder-jammy-tiny:0.0.175\"},{\"builder-image\":\"paketobuildpacks/builder-jammy-tiny:0.0.173\"}]}" >> $GITHUB_OUTPUT
  job2:
    needs: job1
    runs-on: ubuntu-latest
    strategy:
      matrix: ${{ fromJSON(needs.job1.outputs.matrix) }}
    steps:
      - run: |
          echo "Project: ${{ matrix.project }}"
          echo "Project: ${{ matrix.builder-image }}"
  runner:
    #needs: [ builder ]
    runs-on: ubuntu-latest
    #strategy:
    #  matrix:
    #    cfg: ${{ fromJson(needs.builder.outputs.matrix) }}
    steps:
      - id: env-setup
        run: |
          echo "env_token=production" >> $GITHUB_OUTPUT
          echo "env_domain=example.com" >> $GITHUB_OUTPUT
      - run: |
          echo "Token: ${{ steps.env-setup.outputs.env_token }}"
          echo "Domain: ${{ steps.env-setup.outputs.env_domain }}"