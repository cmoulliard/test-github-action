name: WorkflowB

on:
  workflow_run:
    workflows: ["Workflow A"]
    types:
      - completed

jobs:
  jobB:
    #needs: workflow_run.workflowA.outputs.jobA
    needs: workflowA.jobA.outputs.myOutput
    runs-on: ubuntu-latest
    steps:
      - name: Step B
        run: |
          echo "Output from Workflow A: ${{ needs.workflow_run.workflowA.outputs.jobA }}"
